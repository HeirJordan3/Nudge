# 👉 Nudge

> Small pushes. Big progress.

Nudge is a lightweight team task manager. Create tasks, assign them to teammates, set due dates, and track progress across a simple kanban board — all saved locally in your browser.

---

## Running the app

```bash
npm install
npm run dev
```

Then open [http://localhost:5173](http://localhost:5173).
